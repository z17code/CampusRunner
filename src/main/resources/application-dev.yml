# ============================================================
# 校园跑腿代购系统 CampusRunner —— 开发环境配置（dev）
# ============================================================
# 本地开发数据库连接，账号密码直接写死，方便快速联调。
# 激活方式：IDE 启动参数加 --spring.profiles.active=dev，
#          或在 application.yml 临时把 spring.profiles.active 改为 dev。
# ------------------------------------------------------------

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_runner?serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: a1234567
    driver-class-name: com.mysql.cj.jdbc.Driver

# 开发环境（Windows）文件上传存储根目录
campus:
  upload:
    path: C:/uploads

# ============================================================
# AI 智能发单 Agent —— 大模型配置（商汤 sensenova 开放平台）
# 敏感凭证仅写在 application-dev.yml，禁止提交到版本控制。
# base-url 采用 OpenAI 兼容协议（/v1/chat/completions）。
# ============================================================
hp:
  ai:
    agent:
      key: sk-2oCOerZqGTIzGmmw5XQj2iiVhZICBqzF
      base-url: https://token.sensenova.cn/v1/chat/completions
      model: deepseek-v4-flash
